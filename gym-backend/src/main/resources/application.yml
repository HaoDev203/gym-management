spring:
  application:
    name: gym-backend
  profiles:
    active: dev
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: Asia/Shanghai

server:
  port: 8080

logging:
  level:
    root: INFO
    com.gym: DEBUG
