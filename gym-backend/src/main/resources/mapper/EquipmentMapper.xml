<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gym.mapper.EquipmentMapper">

    <resultMap id="BaseResultMap" type="com.gym.entity.Equipment">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="quantity" property="quantity"/>
        <result column="location" property="location"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, name, quantity, location, created_at, updated_at
    </sql>

    <select id="selectById" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM equipment
        WHERE id = #{id}
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM equipment
        ORDER BY created_at DESC
    </select>

    <insert id="insert" parameterType="com.gym.entity.Equipment" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO equipment (name, quantity, location)
        VALUES (#{name}, #{quantity}, #{location})
    </insert>

    <update id="update" parameterType="com.gym.entity.Equipment">
        UPDATE equipment
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="quantity != null">quantity = #{quantity},</if>
            <if test="location != null">location = #{location},</if>
            updated_at = CURRENT_TIMESTAMP
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="java.lang.Long">
        DELETE FROM equipment WHERE id = #{id}
    </delete>

</mapper>
